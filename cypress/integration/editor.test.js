describe('renders the homepage', () => {
    it('renders correctly', () => {
        cy.visit('http://localhost:3000');
        // .type('{movetostart}').type('Hello World');
        // .should('contain.text', 'Hello World');
        // cy.findByRole('textbox').should('have.attr', 'contentEditable', 'true');
        // cy.get('.toolbar-link').click();
        // cy.get('.toolbar-image').click();
    });
});
